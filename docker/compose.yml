version: '3.8'
services:
  dockyard:
    build: .
    ports:
      - "8787:8787"
    volumes:
      - ../data:/app/data
    environment:
      - PORT=8787
    healthcheck:
      test: ["CMD", "/app/docker/healthcheck.sh"]
      interval: 10s
      timeout: 3s
      start_period: 5s
